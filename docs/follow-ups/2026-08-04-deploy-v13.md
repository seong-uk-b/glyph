# v1.3 배포 및 배포 경로 정리

**상태**: in-progress
**우선순위**: P2 (보통)
**예상 공수**: ~15분
**의존성**: 없음

## 문제

배포를 시도하다 **배포 경로가 두 개로 갈라져 있는 것**을 발견했다 (2026-08-04):

1. **실제 경로** = `.github/workflows/deploy.yml` — `main` push 시 Actions 가 빌드해
   Pages 아티팩트로 배포 (`gh api .../pages` 의 `build_type: workflow`)
2. **죽은 경로** = `npm run deploy` (gh-pages 패키지) — gh-pages 브랜치에 push 하지만
   Pages 가 그 브랜치를 읽지 않으므로 **아무 효과 없음**

게다가 1번이 `npm ci` 락파일 검증 실패로 깨져 있어서, push 해도 사이트는 v1.0 을
계속 서빙하고 있었다 (사이트는 200 을 반환하므로 겉으로는 정상으로 보였다).

## 조치 (완료분)

- [x] 워크플로우 Node 20 → 22, `npm ci` → `npm install` (커밋 b599454)
- [x] `docs/harness/verification.md` 배포 검증 절을 실제 경로 기준으로 교정

## 남은 작업

1. **`npm run deploy` 스크립트 처리 결정** — 셋 중 하나:
   - 제거 (권장: 죽은 경로를 남기면 다음 세션이 또 속는다)
   - 유지하되 package.json 에 주석성 이름 변경 (`deploy:legacy`)
2. **gh-pages 브랜치 정리** — 5,781 파일(mp3 포함 ~75MB)이 읽히지도 않는 채 남아 있다.
   삭제하면 저장소 용량 회수. `git push origin --delete gh-pages`
3. 아이폰 실기기 재설치 (Xcode ▶) — mp3·신규 기능 포함 빌드로 갱신

## 참고

- 배포 검증 절차: `docs/harness/verification.md` "배포 검증"
- 실패 로그 확인: `gh run view <id> --log-failed`
